# UI — Dashboard restyle plan

Status: **not started** — to be written after the landing restyle
([`landing/ui-landing.md`](landing/ui-landing.md)).

The dashboard reuses the landing's foundations (Montserrat 800/900 headlines, Inter body,
the same color tokens and React Bits setup) defined in `ui-landing.md` §1.

Once the dashboard is restyled, re-capture the screenshots used by the landing's dashboard
showcase (`src/assets/screens/*.svg`, see `ui-landing.md` §2.6).
